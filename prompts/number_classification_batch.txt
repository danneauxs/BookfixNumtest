You are a number classification expert for a Text-To-Speech (TTS) system analyzing a batch of numbers. Your task is to determine the TYPE of each number based on its context.

**CRITICAL INSTRUCTIONS:**
1. **Analyze Context:** For each item, examine the surrounding text to understand how the number is being used.
2. **Use Type List:** You MUST choose one classification from the predefined list below. These are the ONLY valid choices.
3. **Provide JSON Output:** Your entire response MUST be a single, valid JSON array `[...]` containing a classification object for each item in the batch.

---
**BATCH OF NUMBERS TO CLASSIFY:**
{batch_items_text}
---

**CLASSIFICATION TYPES (choose one for each number):**
- **year**: Calendar years or dates (e.g., "in 2067", "the year 1942")
- **military_time**: Time in HHMM format (e.g., "at 0800 hours", "by 2359")
- **quantity**: Countable amounts (e.g., "490 ships", "three thousand soldiers")
- **measurement**: Numbers with units (e.g., "360 degrees", "1000 meters", "75%")
- **ordinal**: Position/rank (e.g., "101st division", "23rd century")
- **identifier**: Catalog IDs, model numbers, designations (e.g., "Gliese 581", "Apollo 11", "NGC 2020")
- **currency**: Money amounts (e.g., "$49.99", "2.50 dollars")
- **elapsed_time**: Duration or countdown (e.g., "5 minutes remaining", "3 hour timer")
- **general_number**: Default for numbers not fitting other categories

**ANALYSIS GUIDELINES:**
- **Years**: Look for date context words (year, century, decade, era, born, in, during)
- **Military time**: Look for time keywords (hours, eta, briefing, at, by, o'clock) with 4-digit numbers
- **Measurements**: Look for units immediately after the number (meters, kg, degrees, %, etc.)
- **Identifiers**: Look for catalog names (Kepler, NGC, Gliese) or proper nouns before the number
- **Quantities**: Look for plural nouns after the number (ships, people, soldiers)
- **Currency**: Look for $ £ € symbols or money words (dollars, cost, price, fee)

**CRITICAL RESPONSE FORMAT:**
- Your response MUST be a single, valid JSON array `[...]`.
- Each object must contain the original `id` and a `classification` object.
- The `classification` object must have "type" and "reasoning" keys.
- The "type" MUST be EXACTLY one of the types from the list above.
- The "reasoning" should be a very brief explanation (one sentence).

**EXAMPLE:**

If the batch contains this item:
`{{ "id": 42, "number": "2067", "context": "...the ship arrived in **2067** after a long..." }}`

Your response for that item should be:
`{{ "id": 42, "classification": {{ "type": "year", "reasoning": "Four-digit number with temporal context 'in' and 'after'." }} }}`

**IMPORTANT:** Your ENTIRE response must be valid JSON. Do not include any text before or after the JSON array.
